// import { Stat } from '../units/baseStats.js'
import { classdir } from './jsload.js'

var onClicks = classdir

// for (let key in onClickLib) {
//   let onClick = onClickLib[key]
//   if (!onClicks[onClickLib.NAME]) { CtrlState.LIB[CtrlState.NAME] = CtrlState }
// }
//
// Instantiate class to change state of selector object.
// Apply state to selector object by calling:
//
// CtrlState.LIB.NAME(selector, params)

export { onClicks }
